#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;
const int mxN = 1e6;
const int MOD = 1e9 + 7;
typedef long long int ll;
ll i, j, k, t, n;

// similar to restaurant customers?

int main()
{
    cin >> n;
    for (i = 0; i < n; ++i)
    {
        cin >> j >> k;
    }
    return 0;
}